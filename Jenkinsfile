pipeline {
    agent any
    stages {
        stage('Java Build') {
        	steps {
        		echo "java build"
        	}
        }
        stage('docker Build') {
			steps {
				echo "docker build"
        	}
        }
        stage('Deployment') {
			steps {
				echo "Deployment"
        	}
        }

    }
}
